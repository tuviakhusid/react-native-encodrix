query GetUploadedInvoiceStatus($taskIds: [String!]!) {
  multipleTasksStatus(taskIds: $taskIds) {
    taskId
    status
    progress {
      current
      total
      stage
    }
    result
    error
  }
}
